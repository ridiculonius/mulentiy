# mulentiy

Telegram bot for tracking account balance and history.

## Installation

```bash
pip install -r requirements.txt
```

## Commands

- **📈 График** – построить график баланса за указанное количество дней (по умолчанию 30).

The project uses `matplotlib` to render charts.
